{
  "name": "deepseek",
  "version": "0.1.3",
  "description": "DeepSeek provider for ZCode: fill in one API key and the plugin provisions the provider, its models, and per-model thinking levels into ZCode's provider store.",
  "author": {
    "name": "andyfanybo"
  },
  "license": "MIT",
  "skills": "skills",
  "commands": "commands",
  "userConfig": {
    "api_key": {
      "type": "string",
      "title": "DeepSeek API Key",
      "description": "在 https://platform.deepseek.com 生成。保存在本机 ZCode 的插件配置里（明文）；也可以留空，改为在「设置 → 模型供应商」的 DeepSeek 供应商里填 Key，插件会复用而不另存副本。两种方式任选其一。",
      "required": false,
      "default": ""
    },
    "base_url": {
      "type": "string",
      "title": "API 基础地址",
      "description": "ZCode 侧使用 Anthropic 格式端点。插件会自动去掉 /anthropic 后缀去拉模型列表。",
      "default": "https://api.deepseek.com/anthropic"
    },
    "overwrite_levels": {
      "type": "boolean",
      "title": "覆盖已有模型的思考档位",
      "description": "默认关闭：只给缺失档位的模型补写，不覆盖你（或 ZCode）已经写好的档位配置。",
      "default": false
    }
  },
  "mcpServers": {
    "deepseek": {
      "command": "node",
      "args": [
        "${ZCODE_PLUGIN_ROOT}/dist/mcp/server.js"
      ],
      "env": {
        "ZCODE_PLUGIN_ID": "deepseek",
        "DEEPSEEK_API_KEY": "${user_config.api_key}",
        "DEEPSEEK_BASE_URL": "${user_config.base_url}",
        "DEEPSEEK_OVERWRITE_LEVELS": "${user_config.overwrite_levels}"
      }
    }
  }
}
